import 'package:flutter/material.dart';

class EventScreenInfo extends StatelessWidget {
  const EventScreenInfo({Key? key, required index}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
