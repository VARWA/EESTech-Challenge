import 'package:flutter/material.dart';

class EventInfoMainScreenWidget extends StatelessWidget {
  const EventInfoMainScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
